## Front-Controller de proyecto Tiendash

- Copiar el archivo que contiene las variables de entorno
    - > `cp example.env .env`
    - Modificar el archivo y colocar sus respectivas variables
    
- Instalar las dependencias de composer
    > `Composer install`